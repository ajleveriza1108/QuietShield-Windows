<UserControl x:Class="QuietShield.App.Pages.SchedulesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <Border Style="{StaticResource InformationBannerStyle}"><TextBlock Style="{StaticResource BodyTextStyle}" Text="SIMULATION ONLY — schedule evaluation is time-zone-safe and creates no task, service, startup entry, or Windows rule." /></Border>
        <Border Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Study, work, bedtime, and custom schedules" />
                <ListView x:Name="ScheduleTable" Margin="0,11,0,0" MinHeight="250" MaxHeight="430" ItemsSource="{Binding ConnectionSchedules}" IsTabStop="True" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" AutomationProperties.Name="Virtualized connection policy schedules">
                    <ListView.ItemTemplate><DataTemplate><Grid MinHeight="68" ToolTip="{Binding ConflictRule}"><Grid.ColumnDefinitions><ColumnDefinition Width="120" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions><TextBlock FontWeight="SemiBold" Text="{Binding Name}" TextTrimming="CharacterEllipsis" /><StackPanel Grid.Column="1"><TextBlock Style="{StaticResource TrimmedValueTextStyle}" Text="{Binding Days}" ToolTip="{Binding Days}" /><TextBlock Style="{StaticResource TrimmedValueTextStyle}" Foreground="{StaticResource SecondaryTextBrush}" Text="{Binding TimeRange}" ToolTip="{Binding TimeZone}" /><TextBlock Style="{StaticResource TrimmedValueTextStyle}" Foreground="{StaticResource MutedTextBrush}" Text="{Binding Policy, StringFormat=Policy: {0}}" ToolTip="{Binding ConflictRule}" /></StackPanel></Grid></DataTemplate></ListView.ItemTemplate>
                </ListView>
                <TextBlock Margin="0,10,0,0" Style="{StaticResource ExplanatoryTextStyle}" Text="Overnight ranges attribute post-midnight time to the preceding selected day. Resume reconciliation compares the last evaluation with current UTC time." />
                <TextBlock Margin="0,7,0,0" Style="{StaticResource ExplanatoryTextStyle}" Text="{Binding DecisionPrecedenceSummary}" ToolTip="{Binding DecisionPrecedenceSummary}" />
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
